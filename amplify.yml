version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - npm run setup-shadcn
    build:
      commands:
        - npm run aceternity-build
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
